# 시스템 규칙과 배경

**왜 이렇게 만들어졌는지**를 적어둔 문서입니다. 코드만 봐서는 알 수 없고, 모르면 다음
작업에서 잘못된 방향으로 가기 쉬운 것들입니다.

자재를 원본 엑셀에서 다시 넣는 일은 `자재이관_주의사항.md`를 보세요.

최종 수정: 2026-08-07

---

## 1. ⚠️ 재고 차감 규칙 — 두 번 뒤집혔던 것

**한진 SPARE · 한진 구매품일 때만 현재재고를 깎습니다.**
보우 / POSCO / BEUMER / 직접 입력은 이력만 남고 재고는 건드리지 않습니다.

우리 소유가 아닌 자재는 우리 재고가 아니기 때문입니다.

**이건 한 번 잘못 바꿨다가 되돌린 규칙입니다.** 처음엔 이대로 만들었는데, 중간에
"현재재고는 출처 상관없이 다 합쳐진 숫자 아니냐"는 추측으로 **전부 차감**하도록
바꿨다가, 2026-07-30 실무자 확인 결과 **처음 방식이 맞다**고 확정되어 되돌렸습니다.
원본 엑셀도 한진 자재 소모분만 재고에서 뺐다는 게 뒤늦게 확인됐습니다.

> **다시 "전부 차감"으로 바꾸자는 이야기가 나오면 이 기록을 먼저 보여주세요.**

관련 코드: `register_usage` (SQL 함수), `web/js/pages/usage.js`의 `MATERIAL_SOURCES`

### ⚠️ 이 규칙은 두 군데에 있습니다 (2026-08-18)

출고를 **고칠** 때는 "예전 출처가 재고를 깎았는지"를 DB가 스스로 알아야 해서,
같은 규칙을 SQL에도 뒀습니다.

| 어디 | 무엇 | 언제 쓰나 |
|---|---|---|
| 화면 | `usage.js`의 `MATERIAL_SOURCES` | 출고 **등록**할 때 `p_deduct_stock`을 정함 |
| DB | `usage_deducts_stock(text)` | 출고 **수정·삭제**할 때 DB가 스스로 판단 |

**한쪽만 고치면 등록과 수정이 서로 다르게 동작해 재고가 조용히 어긋납니다.**
양쪽 주석에 서로를 가리키는 경고를 달아뒀고, `verify_stage3.py`의 `[10-2]`
(한진 → 보우로 바꾸면 재고가 원복되는가)가 어긋남을 잡습니다.

---

## 1-1. 출고 이력은 관리자가 고치고 지울 수 있습니다 (2026-08-18 추가)

사용이력 표에서 **행을 두 번 클릭**하면 팝업이 열립니다. 자재 목록과 같은 방식입니다.

**재고와 수리 건이 함께 따라갑니다.** 화면은 값만 넘기고 계산은 DB가 합니다
(`update_usage` / `delete_usage`). 화면에서 나눠 부르면 중간에 통신이 끊겼을 때
재고만 바뀌고 이력은 그대로인 상태가 생기기 때문입니다.

- **관리자만** 됩니다. 화면에서도 막고 DB 함수에서도 막습니다
  (RPC는 로그인한 사람 누구나 직접 부를 수 있어 화면에만 두면 안 됩니다).
- **수리 반납이 등록된 건은 못 고칩니다.** 반납이 이미 재고를 되돌려놨는데 또 되돌리면
  두 번 늘어납니다. 반납을 먼저 취소하게 안내합니다.
- 저장 전에 **재고가 어떻게 움직일지 미리 보여줍니다.**

**수리 건이 따라가는 규칙**

| 옛 출처 | 새 출처 | 수리 건 |
|---|---|---|
| 한진 (수리건 있음) | 한진 | 갱신 |
| 한진 (수리건 있음) | 보우 | 삭제 |
| 보우 | 한진 | **새로 만듦** |
| 한진 (수리건 없음) | 한진 | 아무것도 안 함 |

마지막 줄이 **2026-07 이관분 4,244건과 2026-08-18에 넣은 211건**입니다. 한진 출처인데
수리 건이 없습니다(`history`에 직접 넣었기 때문). 오타 하나 고쳤다고 수리 목록에 수천
건이 나타나면 안 되므로 만들지 않습니다.

**`repairs.history_id`가 이걸 가능하게 합니다.** 예전에는 수리 건이 "어느 출고에서
나왔는지"를 안 들고 있어서 자재·수량·날짜로 추측해야 했는데, 같은 날 같은 자재를 같은
수량으로 두 번 출고한 기록이 실제로 있어(2026-06-22 `Tail DRUM(Φ89 1150W용)` 2건)
엉뚱한 수리 건을 지울 뻔했습니다.

설계 배경은 `docs/superpowers/specs/2026-08-18-usage-history-edit-design.md`에 있습니다.

---

## 2. 수리 관리는 별도 입력이 없습니다

수리 건은 **출고를 등록할 때 자동으로 생깁니다.** 자재출처가 한진 SPARE·한진 구매품이면
`history` 기록 + 재고 차감 + `repairs` 등록이 **한 묶음**으로 일어납니다.

"수리 관리" 화면은 새로 만드는 곳이 아니라 **현황 조회 + 반납 처리 전용**입니다.

- `repairs`(보낸 건) + `repair_returns`(반납, 1:N) 두 테이블. 한 건이 여러 번 나눠
  돌아올 수 있어 분리했습니다.
- 반납 결과가 **정상복귀면 재고를 다시 더하고, 폐기면 더하지 않습니다.**
- `repairs` 자체는 재고를 건드리지 않습니다(출고 등록에서 이미 깎았으므로 중복 방지).
- 자재목록에 없는 부품은 `material_id`를 비우고 `item_description`에 글자로만 기록합니다.

처음엔 "수리 보내기" 입력 폼을 따로 만들었다가, 사용자 피드백으로 출고 연동 방식으로
바꿨습니다. **입력 폼을 다시 만들자는 제안은 하지 마세요.**

---

## 3. 업무 규칙은 화면이 아니라 DB에 있습니다

`register_usage` / `add_repair_return` / `receive_purchase_request` /
`remove_purchase_request` 같은 SQL 함수와 RLS 정책이 재고 계산·권한 검사를 담당합니다.

**화면에서 규칙을 다시 구현하지 말고 기존 함수를 부르세요.** 그래야 Streamlit이든
웹이든 같은 규칙으로 동작하고, 중간에 실패해서 일부만 반영되는 일이 없습니다.

---

## 4. 권한

| | |
|---|---|
| **관리자** | 개인별 계정 (예: `gyjeong@hanjin.com`) |
| **일반** | **공용 계정 하나를 여러 명이 공유** |

삭제·수정은 관리자만 하므로 관리자 계정만 개인화하면 충분하다고 판단했습니다.
**일반 사용자 개인 계정 전환은 별도 요청이 없으면 제안하지 마세요.**

최상위 권한자는 `gyjeong@hanjin.com` 한 명이며, **감사 로그 조회**와
**BOQ 추가·삭제**가 이 계정 전용입니다.

화면에서 숨기는 건 편의일 뿐 **실제 차단은 RLS**가 합니다.

### ⚠️ 권한은 `app_metadata`에 있습니다 (`user_metadata` 아님)

`user_metadata`는 **로그인한 본인이 브라우저에서 직접 고칠 수 있는** 칸입니다. 거기에
권한을 두면 일반 계정이 한 줄로 스스로 관리자가 됩니다(2026-08-11에 발견해 이전 완료).
`app_metadata`는 `service_role` 키로만 쓸 수 있어 본인이 못 고칩니다.
**RLS 정책·트리거·`web/js/auth.js`·`manage_users.py`가 전부 이 칸을 봅니다.**

### 강등은 즉시 반영되지 않습니다 (최대 1시간)

Supabase의 신분증(JWT)은 한 번 발급되면 **만료 전까지 되돌릴 수 없습니다.** 서버에
취소 목록이 없어 원리상 즉시 무효화가 안 됩니다. 관리자를 일반으로 내려도 그 사람이
들고 있는 신분증이 갱신될 때까지 관리자 권한이 유지됩니다.

- 본인이 **로그아웃 후 재로그인**하면 즉시 반영됩니다.
- 퇴사·사고처럼 급하면 `manage_users.py`가 **차단(ban)** 을 물어봅니다. 새 로그인과
  신분증 갱신이 즉시 막힙니다(이미 들고 있는 신분증은 만료 전까지 유효).
- 창 자체를 줄이려면 Supabase의 JWT 만료 시간 설정을 낮추면 됩니다. 대신 갱신이
  잦아집니다.

### 로그인 후 1시간 지나면 자동 로그아웃 — 앱 코드로 직접 구현 (2026-08-11)

Supabase 대시보드의 **"Time-box user sessions"**(로그인 후 일정 시간 지나면 강제
로그아웃)는 **Pro(유료) 플랜 기능**이라 무료 플랜에서는 켤 수 없습니다(확인함,
"Inactivity timeout"도 마찬가지). 그래서 서버 설정이 아니라 **`web/js/auth.js`에
직접 구현**했습니다.

- 로그인 성공 시 `localStorage`에 `login_at`(로그인 시각)을 남깁니다.
- 1분마다 + 페이지를 열 때마다 경과 시간을 확인해서, **`MAX_SESSION_HOURS`(현재
  1시간)를 넘으면 자동 로그아웃**합니다(`enforceMaxSession()`).
- **시간을 바꾸고 싶으면 `auth.js`의 `MAX_SESSION_HOURS` 숫자만 고치면 됩니다.**
- 이 기능이 생기기 전부터 로그인해 있던 세션(기준 시각 없음)은 만료로 보지 않습니다.

**서버가 강제하는 게 아니라 브라우저 코드로 도는 검사**입니다. 개발자 도구로 일부러
우회하지 않는 이상 실사용에서는 차이가 없습니다. 이 앱의 최종 방어선은 어차피
공용 비밀번호 강도입니다.

**끊기는 경우:** 1시간 경과 / 로그아웃 버튼 / 브라우저 저장공간 삭제 / 다른 브라우저·
기기 접속. `web/PORTING.md`의 "세션 유지"는 **"1시간 안에서는" 유지**로 갱신 필요.

---

## 5. ⚠️ 보안 — "사내망이니 괜찮다"는 가정 금지

이 앱은 **인터넷에서 접속 가능합니다.** 누구나 로그인 화면에 도달할 수 있습니다.

**가장 약한 고리는 공용 계정 비밀번호 강도입니다.** 일반 계정을 여러 명이 비밀번호
하나로 공유하기 때문입니다.

### 접속자 IP를 알 수 없습니다 (다시 확인하지 마세요)

리버스 프록시 뒤라 모든 접속자가 루프백(`::ffff:127.0.0.1`)으로 보입니다.
`X-Forwarded-For`도 못 씁니다 — 내부망 주소인 데다 **같은 기기에서 새로고침할 때마다
값이 바뀌고**, 다른 기기가 같은 값을 받기도 했습니다. `X-Real-Ip`는 항상 없습니다.

그래서 IP 기준 잠금·속도 제한·접속 통계는 이 환경에서 불가능합니다.
Streamlit 앱의 로그인 잠금은 **꺼져 있고**, 웹 버전에는 아예 넣지 않았습니다.

**계정 단위로 잠그는 방식은 쓰지 마세요.** 외부인이 일부러 틀려서 팀 전체를 마비시킬
수 있습니다.

### 제대로 막고 싶어지면 (현재 보류)

1. Supabase에 CAPTCHA(hCaptcha/Turnstile) 켜기 — IP 없이도 자동화 공격을 막는 표준 방법
2. 일반 계정도 개인별로 전환 (4번 정책 변경 필요)
3. Supabase Auth 로그에서 실제 이상 시도가 있는지 먼저 확인

### 항상 확인할 것

`.streamlit/secrets.toml`이 저장소에 올라가지 않아야 합니다.
웹 버전 `web/js/config.js`의 키는 `sb_publishable_`로 시작하는 **공개용**이라 공개
저장소에 있어도 괜찮습니다. `sb_secret_`로 시작하는 키는 **절대 넣으면 안 됩니다.**

---

## 6. 배포

| | |
|---|---|
| **웹 (지금 쓰는 것)** | `main`에 push → Cloudflare Pages 자동 배포. Build output = `web` |
| Streamlit (삭제됨) | 2026-08-10에 앱 파일을 전부 지웠습니다. Community Cloud 연결이 남아 있으면 push마다 빌드가 실패하니 연결을 끊으세요 |

저장소는 **공개(Public)** 입니다. `git add .`을 쓰지 말고 올릴 것만 지정하세요.

확인: `python verify_web.py --show` (배포된 사이트를 브라우저로 조종해서 검사)

---

## 7. 아직 남은 위험

- **`materials`의 "일반 계정은 현재재고만 수정 가능" 규칙이 트리거
  (`restrict_material_update`) 하나로만 지켜집니다.** 트리거가 지워지면 방어선이 통째로
  사라집니다. RLS로 이중화할 수 있지만 아직 안 했습니다.
- **재고가 음수가 될 수 있습니다.** `adjust_material_qty`에 음수 방지가 없습니다.
  실제로 음수인 자재가 있어서 **막지 않기로 했고**, 대신 표에 빨갛게 표시하고
  출고·수정·구매요청 삭제 직후에 알립니다.

---

## 8. 웹 버전에서 일부러 다르게 한 것

- **부품 선택칸의 값이 부품명이 아니라 자재 id입니다.** 이름이 같은 자재가 있어서
  이름으로 되찾으면 늘 첫 번째 것이 골라져 **엉뚱한 자재의 재고가 깎입니다.**
- **자재 수정은 행을 두 번 클릭**해야 열립니다(구매 요청은 한 번). 표를 훑다가 실수로
  열리는 것을 막기 위함입니다.
- **자재 수정 시 현재재고는 덮어쓰지 않고 "창을 연 시점 대비 차이"만 반영**합니다.
  창을 열어둔 사이 다른 사람이 출고해도 그 변화를 지우지 않습니다. ⚠️ 놓치기 쉬움

화면별 상세 체크리스트는 `web/PORTING.md`에 있습니다.

---

## 9. 성능

BOQ 검색은 전체를 받아와 걸러내지 않고 **DB에서 `find_boq` 함수로 바로 찾습니다**
(3.7배 빠름). 띄어쓰기·대소문자를 무시하고 PLC 그룹 포함/미포함 형태 둘 다 찾습니다.

Supabase는 한 번에 1000건까지만 주므로 그보다 많으면 나눠 받아야 합니다.
**여러 페이지를 동시에 요청할 때 주의** — Streamlit 쪽에서 HTTP/2 경쟁 상태로
`ReadError`가 났던 적이 있어 재시도를 넣어뒀습니다.

---

## 10. 코드 작업 방식 (2026-08-07 정착)

```
코드 수정 → 같은 패턴이 다른 곳에 또 있는지 grep → 커밋 전 /code-review
→ 지적 없을 때까지 반복 → 배포
```

자재 수정 화면 하나에서 **리뷰 6회 동안 28건**이 나왔고 전부 진짜였습니다. 그중 11건은
"앞서 고친 것을 다른 곳에는 안 옮긴" 반복이었습니다.

**배포부터 하고 검토받지 마세요.** 문제 있는 코드가 며칠씩 돌아갑니다.

### 리뷰 이력 (2026-08-07 ~ 08-11)

| 회차 | 대상 | 나온 건수 |
|---|---|---:|
| 1~6차 | 자재 수정/삭제(`materials.js`), 구매요청 삭제(`purchase.js`) | 28 |
| 7차 | `materials.js` · `purchase.js` · `alert.js` | 6 |
| 8차 | 삭제된 Streamlit 앱 참조 정리 | 3 |
| 9차 | `purchase.js` · `register.js` · `db.js` · `main.js` · `boq.js` · `repairs.js` · `supabase_setup.sql` | 9 |
| 10차 | **`supabase_setup.sql` 전체** (DB 보안·재고 정확성·DB 함수·search_path) | 9 |
| 별도 | `manage_users.py` → **권한 상승 취약점 발견** | 4 |
| 별도 | `verify_stage3.py` 8건 · `verify_trigger.py` 5건 (거짓 통과 제거) | 13 |
| 커밋 전 | 1시간 자동 로그아웃(`auth.js`·`main.js`) | 2 |

**전부 실제 버그였습니다.** 오탐으로 걸러진 건 거의 없었습니다.

### ⚠️ 리뷰가 안 닿은 곳 (남은 위험)

**리뷰는 대부분 `git diff` 기준, 즉 "그 커밋에서 바뀐 줄"만 봤습니다.**
파일을 처음부터 끝까지 통으로 본 것은 **`supabase_setup.sql`(10차)뿐**입니다.

그래서 아래는 **한 번도 리뷰 대상이 된 적이 없거나, 바뀐 줄만 스쳐간** 파일들입니다.

| 파일 | 상태 |
|---|---|
| `web/js/pages/usage.js` | 재고 음수 알림 관련 줄만 봤고, 출고 등록 로직 전체는 미검토 |
| `web/js/table.js` | 바뀐 줄만 (더블클릭·음수 표시) |
| `web/js/ui.js` | 바뀐 줄만 (`warn` 종류 추가) |
| `web/js/excel.js` | **한 번도 없음** |
| `web/js/supabase.js`, `config.js` | 짧아서 문제 소지는 적지만 미검토 |
| `web/index.html`, `web/css/style.css` | 부분적으로만 |
| `verify_web.py` | 고치기만 하고 리뷰는 안 받음 |

**사용자에게 열기 전에 `usage.js`(출고 등록)를 한 번 보는 것을 권합니다** — 재고를 깎는
경로라 문제가 나면 조용히 숫자가 틀어집니다. 나머지는 화면에서 바로 티가 나는 것들입니다.
