# 자재 이관 주의사항

`부품관리.xlsx`의 **새 버전을 DB에 다시 넣을 때** 반복해서 적용해야 할 판단들입니다.
그냥 다시 넣으면 예전에 고쳤던 문제가 되살아납니다.

> 재고 차감 규칙·권한·보안처럼 **시스템 전반의 배경**은 `시스템_규칙과_배경.md`에 있습니다.

최종 수정: 2026-08-07

---

## 1. 전기 ↔ 스위치 시트에 같은 부품이 중복으로 적혀 있음 (6건)

**두 시트에 같은 부품이 각각 적혀 있습니다. 스위치 쪽으로만 등록하고, 전기 쪽 6줄은 등록하지 마세요.**

| 규격(Model) | 전기 시트 | 스위치 시트 |
|---|---|---|
| `XB5 AVB6` / `XB5AVB6` | 창고 36 · Indicator Light BLUE, CP, INV, GHP, MS PANEL | No.19 · 표시 램프(청색, 24V, ø22) |
| `XB5 AW33B5` / `XB5AW33B5` | 창고 37 · Illuminated Pushbutton Green, ... | No.24 · 시작 버튼 (녹색, 조광) |
| `XB5 AW34B5` / `XB5AW34B5` | 창고 38 · Illuminated Pushbutton Red, ... | No.21 · 버튼 (적색, 조광) |
| `XB5 AW35B5` / `XB5AW35B5` | 창고 39 · Illuminated Pushbutton Yellow, ... | No.20 · 리셋 버튼 (황색, 조광) |
| `XB5 AW31B5` / `XB5AW31B5` | 창고 39 · Illuminated Pushbutton White, ... | No.22 · 허가 버튼 (흰색, 조광) |
| `XB5 AW36B5` / `XB5AW36B5` | 창고 39 · Illuminated Pushbutton Biue, CS122 | No.23 · 해제 버튼 (청색, 조광) |

> ⚠️ **두 시트의 모델 표기가 다릅니다.** 전기는 `XB5 AVB6`(띄어쓰기 있음), 스위치는 `XB5AVB6`(붙여씀).
> 자동으로 중복을 찾으려면 **공백을 지우고 대문자로 바꿔서** 비교해야 합니다.

2026-08-07 기준 DB에는 스위치로만 들어가 있습니다. 전기 시트 6줄을 원본에서 지울지는
실무자 확인 대기 중입니다. 지워지면 이 항목도 필요 없어집니다.

---

## 2. 전기 시트는 열이 밀려 들어가기 쉬움

2026-08-07에 세 칸이 한 칸씩 밀린 것을 고쳤습니다. 다시 넣을 때 아래대로 넣으세요.

| DB 칸 | 원본 `자재 현황(전기)` 열 |
|---|---|
| `part_name` (부품명) | **규격(Model)** |
| `install_location` (설치위치) | **설치 위치 및 종류** |
| `sub_type` (구분) | **구분** |

**시트마다 열 구성이 다릅니다.** 전기는 `설치 위치 및 종류 | 규격(Model)` 두 칸이지만,
스위치는 `설치 위치 | 종류 | 규격(Model)` 세 칸입니다. 시트별로 매핑을 따로 확인하세요.

### 밀림을 찾는 방법

**원본 각 열의 빈 칸 개수를 세어 DB 칸의 빈 칸 개수와 대조**하면 찾을 수 있습니다.
전기에서 `install_location` 빈 칸이 4건이었는데, 원본에서 빈 칸이 4건인 열은
`구분` 하나뿐이라 바로 확정됐습니다. (창고No 79, 종류 0, 규격 0, 제조사 6)

---

## 3. 부품명을 규격으로 바꾸면 안 되는 8건

규격 칸에 모델번호가 아니라 **설명**이 들어 있습니다. 이 8건은 부품명을 그대로 두세요.

| 원본 순번 | 부품명으로 쓸 값 (설치 위치 및 종류) | 규격 칸 (쓰면 안 됨) |
|---|---|---|
| 115 | FUSE | `FUSE` |
| 218 | 체적 장비(부피측정기) | `체적 장비(Volumec HD)` |
| 219 | 카메라 (VICAM 3S) | `카메라(Vicam 3S)` |
| 222 | 3분류 IRS용 PC 용 파워 서플라이 **FSP850-50FGPH3** | `IRS PSU` |
| 223 | 3분류 IRS용 PC 용 Mainboard | `ISR Mainboard` |
| 224 | 3분류 IRS용 PC 용 CPU | `IRS CPU` |
| 225 | 3분류 IRS용 PC 용 Cooler | `IR Cooler` |
| 227 | FR컨베이어 UP/DOWN 실린더 이물질 방지용 자바라 | `실린더 커버` |

222번은 **부품명 쪽에 진짜 모델번호가 있습니다.**

---

## 4. 합친 자재 3종 (전기)

같은 부품이 창고별로 따로 적혀 있어서 한 줄로 합쳤습니다. 실무자 확인을 받았습니다.
새로 넣을 때도 똑같이 합치세요.

| 부품명 | 창고번호 | 현재재고 |
|---|---|---|
| `NZ2GN2S1-32TE` | `16, 41, 42` | 2+2+2 = 6 |
| `LCP 102` | `47, 48, 49` | 27+33+4 = 64 |
| `US-7 1.0CL 15VA` | `74, 75` | 1+1 = 2 |

**합치기 규칙:** 창고번호는 한 칸에 쉼표로 모아서 / 현재재고는 합산 /
**표준재고는 합산하지 않고 남길 행 값 유지**

---

## 5. 원본이 원래 이상한 것들 (고치지 말 것)

시스템 오류가 아니라 **원본 엑셀이 그렇게 되어 있습니다.** 그대로 두기로 했습니다.

### 글자가 잘린 9건 (전기)
```
순번 116, 117, 130, 131, 151   "0AF, 50AT 4P, 50kA 415/460V W:SHT DC110"
                               → 앞 숫자가 잘림 (100AF 로 추정)
순번 132, 133, 134, 135        "nsformer, 100A,..." "sformer, 250A,..." "ormer, 500A,..."
                               → Transformer 앞부분이 잘림
```

### 그 외
- 순번 101 부품명 칸에 메모가 들어감 — `GL6-P1112 과 호환여부 확인 필요 / 호환가능 확인완료`
- 모터 창고 `44-4` `MS90L-4` 발주코드가 **`?`** — 실무자도 모름
- 모터 창고 `44-3` 품목명에 `(수정필요)` 표기 있음

---

## 6. 원본 시트 읽을 때

- **머리글은 3번째 줄** (`pandas` 기준 `header=2`)
- 머리글 바로 아래에 **숫자만 있는 잔재 행 1개**가 있으니 버려야 함
- **모터 시트는 머리글이 2줄**
- **창고번호는 시트마다 1번부터 다시 시작**하므로 카테고리+창고번호를 함께 키로 쓸 것
- 모터 `44-1`, `44-2`는 한 칸에 두 품목이 들어 있어 자동 대조 불가

---

## 7. 다른 카테고리 점검 결과 (2026-08-07)

**전기 같은 "칸 밀림"은 다른 카테고리에 없습니다.** 대신 **원본 열이 통째로 안 들어간 누락**이
있었고, 그중 사용자가 하기로 한 273건은 반영했습니다(아래 표 참고).

### 확인된 누락과 처리 결과 (2026-08-07)

| 카테고리 | 원본 열 | 건수 | 처리 |
|---|---|---:|---|
| 벨트류 | 종류 → 구분 | 193 | ✅ 반영 |
| 외산(TAMS) | 구분 | 42 | ✅ 반영 (원본에 있는 것만) |
| 외산 | 구분 | 38 | ✅ 반영 |
| 롤러/풀리/스프라켓 | 발주 CODE | 153 | ⏸ **사용자 결정: 필요 없음.** 필요해지면 그때 |
| 외산 | 발주 CODE | 38 | ⏸ **사용자 결정: 필요 없음.** 필요해지면 그때 |
| 모터 | 발주 CODE 6건 차이 | 6 | ⏸ 하지 않음 |
| 롤러/풀리/스프라켓 | 규격(Posco 제공) | 79 | ⏸ 하지 않음 (부품명이 이미 있어 급하지 않음) |

**벨트류는 부품명도 고쳤습니다.** DB에 `종류 + 부품명`이 붙어 들어가 있던 것을 떼어냈습니다.

```
부품명 'Round Belt ø6*330'  →  부품명 'ø6*330' + 구분 'Round Belt'
```

**비고는 원본 `비고`(48열)와 `수정 내역`(49열)을 ` / `로 합쳤습니다.** 벨트류 시트는
머리글이 세 줄이고 오른쪽 끝에 이 두 열이 있습니다. 가운데의 `*참고사항`은 숫자 칸이라
비고가 아닙니다 — 헷갈리기 쉬우니 주의.

**원본이 한 칸에 줄바꿈으로 여러 값을 담은 곳은 ` / `로 나눠 넣었습니다** (외산의 구분·적용 C/V).

```
'Type A\nType C'  →  'Type A / Type C'
```

백업 테이블: `materials_backup_20260807b` (620건)

### 누락이 아닌 것 (건드리지 말 것)

- **외산(TAMS) `품목명(한글)`** — 392행 전부 `~` 플레이스홀더라 실제 데이터가 없음. 부품명은 `품목명(영문)`이 맞음.
- **베어링/바퀴, 스위치의 `종류`** — 부품명에 흡수돼 있음. 예) `체인 HC50`(종류 `체인` + 규격 `HC50`), `풋 스위치 HRF-MD2Y`. 유실 아님.

### 행 수 차이 — 2026-08-07 전부 확인 완료

| 카테고리 | 원본 | DB | 결론 |
|---|---:|---:|---|
| 모터 | 55 | 55 | **누락 아님.** 제가 잘못 셌던 것 |
| 스위치 | 44 | 41 | **단종품 3건을 뺀 것. 그대로 둡니다** |
| 외산(TAMS) | 392 | 382 | **중복 합치기로 지운 10건이 맞음** |

**모터 — 세는 법 주의.** 이 시트는 머리글이 2줄이라 잔재 행이 한 칸 밀려 있어서
`header=1` + `iloc[1:]` 만으로는 안 걸러집니다. 아래처럼 숫자만 있는 행을 또 빼야
원본이 55행으로 정확히 나옵니다(부품명은 5열, 설치위치는 4열).

```python
m = pd.read_excel(X, sheet_name="자재 현황(모터)", header=1).iloc[1:]
m = m[m.iloc[:, 5].notna()]
m = m[~m.iloc[:, 4].astype(str).str.fullmatch(r'\d+(\.\d+)?')]   # 합계 줄 제거
```

**스위치 — 등록하지 않는 3건 (단종, 사용자 확인).** 새로 이관해도 넣지 마세요.

| 원본 No | 설치위치 | 종류 | 규격 |
|---|---|---|---|
| 12 | CS BOX | Orange Lamp Block(단종) | `ZBV-B5` |
| 13 | CS BOX | Blue Lamp Block(단종) | `ZBV-B6` |
| 14 | CS BOX | Green Lamp Block(단종) | `ZBV-B3` |

**외산(TAMS) -10건.** 2026-08-06 중복 합치기로 지운 id
`891,938,977,1001,1045,1064,1076,1078,1085,1140`이며, 전부 TAMS의 id 구간
(`812 ≤ id < 1204`)에 들어갑니다. 데이터 유실이 아닙니다.

> 카테고리별 id 시작: 전기 528 / 외산(TAMS) 812 / 외산 1204

### 카테고리 ↔ 시트 대응

```
롤러/풀리/스프라켓 ← 자재 현황(보우-롤러풀리스프라켓)   185건 일치
벨트류             ← 자재 현황(벨트류)                 199건 일치
베어링/바퀴        ← 자재 현황(베어링바퀴)              81건 일치
외산               ← 자재 현황(외산)                    39건 일치
모터 / 스위치 / 외산(TAMS) / 전기 는 위 표 참고
```

### 열 매핑 (확인된 것)

| 카테고리 | 부품명 | 설치위치 | 구분 |
|---|---|---|---|
| 롤러/풀리/스프라켓 | 규격(SPARE PART NAME) | 적용 C/V | 구분 ✅ |
| 벨트류 | 부품명 | 적용 C/V | (비어있음) |
| 베어링/바퀴 | 종류 + 규격(Model) | 설치 위치 | (비어있음) |
| 스위치 | 종류 + 규격(Model) | 설치 위치 | (비어있음) |
| 모터 | 모델 | 적용 C/V | (비어있음, 원본에 열 없음) |
| 외산(TAMS) | 품목명(영문) | 적용 C/V | (비어있음) |
| 외산 | 품목명(영문) 또는 설명 | 적용 C/V | (비어있음) |
| 전기 | 규격(Model) | 설치 위치 및 종류 | 구분 ✅ |

---

## 8. 이전에 이관하면서 발견했던 것들 (2026-07)

이 문서는 원래 전기 자재 정리(2026-08-07)에서 시작했지만, 그 전에 겪은 이관 문제도
같은 종류가 반복됩니다. 새 원본을 넣을 때 다시 겪지 않도록 여기 모읍니다.

### 8-1. 발주코드가 DB에서 오염돼 있던 59건 (복구 완료)

| 대상 | 건수 | 증상 |
|---|---:|---|
| 외산(TAMS) | 20 | 부품명이 같은 그룹에서 **마지막 행의 발주코드가 그룹 전체에 복사**됨 |
| 모터 | 39 | 발주코드가 아예 `null` (이관 시 누락) |

예) `PRE-ASSEMBLED IP-LINK CABLE` 8건이 원본은 7종인데 DB엔 전부 `7120708`(창고321 값).

**⚠️ DB 값이 이상하게 균일하면 원본부터 대조할 것.** 이때 나는 DB만 보고
"24종 전부 발주코드가 같다"고 잘못 보고했고, 실무자(장필교)가 "발주코드 상이함"이라고
짚어줘서 바로잡았습니다.

복구 못 한 1건: **모터 창고 `44-4` `MS90L-4`** — 원본 엑셀의 발주코드가 `?`.
(창고 `44-3`도 원본 품목명에 `(수정필요)` 표기가 있음)

### 8-2. 부품명이 같은 자재 합치기 (2026-08-06, 10건 삭제)

전체 24종 64건 중 실제로 합칠 것은 **9종 10건뿐**이었고 나머지는 이름만 같은 별개
품목이었습니다. 지운 id `891,938,977,1001,1045,1064,1076,1078,1085,1140`(전부 TAMS).

**합치기 규칙:** 남길 행은 id 최솟값 / 창고번호는 한 칸에 쉼표로 모음 /
현재재고는 합산 / **표준재고는 합산하지 않고 남길 행 값 유지**(사용자 결정).

**남은 중복 16종 34건은 정상입니다. 다시 합치려 하지 마세요.**
발주코드가 전부 다른 8종(`PRE-ASSEMBLED IP-LINK CABLE` 7건 등)과, 원본에 발주코드
컬럼 자체가 없는 전기 8종(실무자가 "각기 다른 부품"으로 확인해줌).

### 8-3. 창고번호 (2026-07-16, 완료)

`warehouse_no` 컬럼을 만들어 **모터/전기/외산(TAMS) 687건 중 588건**을 채웠습니다.
나머지 99건은 원본에도 창고번호가 비어 있어 안 채운 것 — 정상입니다.

### 8-4. 구매이력 수량이 통째로 틀렸던 건 (2026-07-23, 169건 정정)

원본 "구매이력" 시트에는 **`구매수량`(실제 산 개수)과 `표준재고`(유지 기준치)가 별도
컬럼**인데, 이관할 때 `표준재고`를 구매수량 자리에 넣었습니다. 시트 앞부분은 병합셀
때문에 우연히 맞았고 뒷부분부터 거의 전체가 틀려 있었습니다.

**원본 정보가 없어 못 고친 5건:** id `4520, 4776, 4811, 4886, 5032`.

#### 어느 칸이 수량인지 (2026-08-18 확인, 실무자 확인 완료)

"앞부분은 우연히 맞았다"고 적었는데 우연이 아니었습니다. **시트 앞부분은 `구매수량` 칸이
아예 비어 있고 `표준재고` 칸에 구매수량이 들어 있습니다.** 나중에 칸이 추가되면서
그때부터 `구매수량` 칸에 제대로 적히기 시작한 것입니다.

경계가 깔끔하게 갈립니다(2026-08-18 최신본 기준 행번호).

| 구간 | 구매수량 칸 | 수량이 있는 곳 | 건수 |
|---|---|---|---:|
| ~902행 | 전부 비어 있음 | **표준재고 칸** | 588 |
| 908행~ | 전부 값 있음 | **구매수량 칸** | 256 |

행번호는 원본이 바뀌면 밀리므로 **번호 대신 이 규칙을 쓰세요.**

```
수량 = 구매수량 칸이 비어 있으면  →  표준재고 칸 값
       구매수량 칸에 값이 있으면  →  구매수량 칸 값
```

`계 ÷ 단가`로 역산해 검증할 수 있는 764건 중 **762건(99.7%)이 이 규칙과 일치**합니다.
단가나 계가 없어 검증할 수 없는 80건도 두 칸 중 하나는 차 있어서 규칙이 답을 줍니다.

**규칙과 어긋나 보였던 2건 — 실무자 확인 완료, 구매수량 칸이 맞음**

| 행 | 규격 | 구매수량 칸 | 표준재고 칸 | 계÷단가 |
|---|---|---:|---:|---:|
| 1454 | `PHS10R+샤프트75L+PHS10L+조립` | **25** | 100 | 26 |
| 1456 | `Tail DRUM(Φ89 930W용 B/F 830)` | **8** | 10 | 10 |

> ⚠️ **`계 ÷ 단가`는 검증 보조 수단일 뿐, 정답이 아닙니다.**
> 이 2건은 **그 업체에 줄 미지급금이 있어 금액에 얹어 정산한 것**이라 금액이 실제
> 구매수량보다 큽니다(실무자 확인, 2026-08-18). 금액에는 운송비·정산분이 섞일 수
> 있으니, **금액과 수량이 어긋나면 금액이 아니라 수량 칸을 믿으세요.**

> ⚠️ **최근 것만 보고 규칙을 정하지 마세요.** 2026-08-18에 신규 구매 28건만 대조하고
> "구매수량 칸이 맞다"고 결론 냈다가 실무자가 바로잡아 줬습니다. 28건이 전부 아래쪽
> 구간이라 그렇게 보였을 뿐이고, 시트 전체로는 표준재고 칸(588건)이 오히려 다수입니다.

### 8-5. "비고 한 칸에 여러 정보가 뭉쳐 있던" 패턴 (반복됨)

구매이력과 사용이력 **둘 다** 이랬습니다.

```
비고: "문제:X / 조치:Y / 부품:Z"      ← 각 컬럼으로 나뉘어야 할 것이 한 칸에
```

사용이력 43건을 정규식으로 파싱해 문제/조치/부품메모로 옮겼습니다(2026-07-30).
**새로 이관할 때 비슷한 게 보이면 같은 방식으로 나눌 것.**

### 8-6. 사용(출고) 이력은 검증 완료 (2026-07-23)

원본 "교체이력" 시트와 4,245건 전수 대조 — **건수·수량 100% 일치.**
설비ID가 빠진 16건만 사용자가 직접 확인해 보정했습니다. **다시 전수 검증할 필요 없습니다.**

원본에 수량이 `-60`인 특이 행이 하나 있는데(서보암 핀) 원본 기록 그대로라 두었습니다.

### 8-7. 아직 안 옮긴 원본 정보

- **주/야 구분** — 원본 교체이력에 있으나 DB에 컬럼 없음. 필요성부터 다시 물어볼 것.
- **입고/보류 상태** — 재고 계산 방식을 원본과 다르게 가기로 확정해서 재현할 필요가 없어짐.
  2026-08-18에 정확한 구조를 확인했습니다(아래 8-8).
- **사용이력의 원본 비고 유실분** — 얼마나 빠졌는지 미확인. (구매이력 쪽은 1,647건 유실 확인됨)

> **원본을 그대로 재현하는 게 항상 정답은 아닙니다.** 재고 계산 방식은 원본과 다르게
> 가기로 정한 사례가 있습니다.

### 8-8. 교체이력에서 "재고가 깎이는 행"을 고르는 법 (2026-08-18)

**자재출처가 한진이라고 다 깎이는 게 아닙니다.** `입고/보류(메모)` 열(18열)까지 봐야 합니다.

| 입고/보류 | `입력X` 열(19열) | 재고 차감 | 행수 | 수량 |
|---|---|---|---:|---:|
| 보류 | `소모` | **깎임** | 2,790 | 3,513 |
| (빈칸) | `소모` | **깎임** | 222 | 228 |
| 입고 | (빈칸) | 안 깎임 | 135 | 142 |
| 완료 | (빈칸) | 안 깎임 | 50 | 76 |

**예외 0건입니다.** `입력X` 열은 따로 입력하는 값이 아니라 `입고/보류`에서 자동으로 나온
것이라, 둘 중 아무거나 봐도 됩니다.

의미: `보류`는 **쓴 자재를 아직 다시 채워넣지 못한 상태**라 재고에서 빠진 채로 있고,
`입고`·`완료`는 보충이 끝나 원래대로 돌아온 것입니다. 그래서 원본의 현재재고는
"아직 보충 안 된 소모분"만 뺀 값입니다.

**`보류` → `입고`로 바뀌는 실제 상황은 "수리 보낸 부품이 돌아온 것"입니다**
(2026-08-18 실무자 확인). 한진 자재를 써서 설비에 장착하면 재고에서 빠지고 `보류`가
되는데, 떼어낸 부품을 수리해서 돌아오면 `입고`로 바꾸고 재고를 되돌립니다. 인수자와
입고일자 칸이 그때 채워집니다. **우리 시스템의 "수리 관리 → 반납(정상복귀)"과 같은
흐름입니다.**

**우리 DB는 이걸 재현할 필요가 없습니다.** 기록 방식만 다르고 결과가 같습니다.

```
원본 : 쓰면 사용열에 기록  →  보충되면 '입고/완료'로 바꿔 사용열에서 뺌   → 순 0
우리 : 쓰면 무조건 차감      →  보충되면 구매 입고로 다시 더함              → 순 0
```

⚠️ **이 열을 빼놓고 대조하면 틀립니다.** 2026-08-18에 한진 출처 행을 전부 합산해
"사용열이 380개 부족하다"고 잘못 보고했습니다. `소모` 기준으로 다시 하니 차이가 **+17개,
8종**으로 줄었습니다. 실무자가 "보류면 차감 아니냐"고 짚어줘서 바로잡았습니다.

남은 8종은 전부 벨트류이고 **사용열이 이력의 정확히 2배**입니다
(`Belt 45 x 1601 i.e.l.` 사용열 12 / 이력 6 등). 원인 미확인.

---

## 10. 2026-08-21 — "돌아온 자재" 73건을 수리 관리에 이관

### 무엇을 했나

원본 교체이력에서 **자재출처가 한진(SPARE/구매품)이면서 `입고`/`완료`인 185건**을
DB의 수리 관리와 대조해, **빠져 있던 73건을 `repairs` + `repair_returns`에 넣었습니다.**
(2026-07-30에 168건을 이관했지만 7/13본 기준이라 그 뒤 것과 그때 빠진 것이 있었습니다.)

```
원본 한진 + 입고/완료   185건
  이미 DB에 있던 것     112건
  새로 넣은 것           73건   ← 수리 건 168 → 241건
재고 변화                0      ← 1,213종 전부 그대로
```

### ★ 재고를 건드리지 않고 넣는 법

**`add_repair_return()`을 쓰면 안 됩니다.** 그 함수는 '정상복귀'일 때 현재재고를 더합니다.
원본의 `입고`/`완료`는 **이미 재고가 복구된 상태**라(8/18 동기화에서 차감 대상에서 제외),
그 함수로 넣으면 재고가 두 번 늘어납니다.

`repairs`·`repair_returns`에 **직접 INSERT**하면 됩니다. 두 테이블에는 **트리거가 없고**,
재고를 바꾸는 `update materials set current_qty`는 전부 함수 안에만 있습니다(7군데 확인).
넣기 전후 재고를 전량 대조해 0 변화를 확인했습니다.

### 넣은 값

| 칸 | 값 |
|---|---|
| `material_id` | 이름이 정확히 하나 맞으면 연결(66건), 아니면 `item_description`에 이름만(7건) |
| `sent_on` / `returned_on` | 원본 사용일 / 입고일자. **반납일이 빈 60건은 사용일로 대신** 넣고 그 사실을 반납 비고에 남김 |
| `reason` | 원본 비고 그대로 ("포스코 대여", "잉여부품 사용" 등) |
| `note` | `[원본 이관 2026-08-21] ... 재고는 이관 시점에 이미 반영되어 있어 반납 처리를 하지 않고 기록만 넣었습니다.` |
| `history_id` | **비움**. 채우면 그 출고 이력이 수정·삭제 불가가 됩니다(반납 가드). 기존 168건과 동일 |

### ⚠️ 알고도 넣은 것 — `완료`는 성격이 다릅니다

`완료` 50건 중 **42건은 반납일도 인수자도 없고** 비고가 *"잉여부품 사용"·"탈거품 사용"·
"파손카트 재활용"·"무상 제공품"*입니다. **물건이 돌아온 게 아니라 애초에 창고 재고를
안 쓴 것**이라 재고가 안 깎인 겁니다. 수리 관리에 넣으면 "나갔다 돌아왔다"는 없던
사실이 생깁니다.

**이 점을 알리고 사용자가 "수량에 영향을 주는 게 아니라 기재만 하는 거면 다 넣자"고
결정했습니다.** 나중에 수리 목록에서 이런 건이 보이면 `reason` 칸(원본 비고)을 보면
구분됩니다.

### ⚠️ Supabase는 한 번에 1,000행만 돌려줍니다

처음 대조에서 `자재: 1000건`으로 나왔습니다(실제 1,213건). 그래서 **있는 자재를
"목록에 없음"으로 12건 잘못 판단**하고, 기존 수리 건 7건도 "빠졌다"고 잘못 셌습니다.
`.range()`로 나눠 읽어 고쳤습니다. **DB 전량을 읽을 때는 항상 페이지를 넘길 것.**

### 원본 필터와 우리 대조가 같은지 확인함

사용자 필터(한진 + 입고/완료 중 `소모` 제외)와 우리가 뽑은 185건이 동일합니다 —
`소모`는 전부 `보류`(2,790)·빈칸(222)에만 있고 **입고/완료에는 0건**입니다.

## 9. 2026-08-18 이관 기록 (최신본 대조)

`Mega-Hub 부품관리(최신).xlsx`(8/18)를 `부품관리.xlsx`(7/13)와 대조했습니다.

**부품명·규격·발주코드가 바뀐 자재는 하나도 없었고, 삭제된 자재도 없습니다.**

### 반영한 것 (5건)

| | id | 내용 |
|---|---:|---|
| 등록 | 1261 | 벨트류 `골 벨트 (metering용 780 * 2660용)` — 기존 `2260용`과 별개 자재 확인함 |
| 등록 | 1262 | 벨트류 `100 * 10,000` |
| 수정 | 550 | 전기 `NZ2EHG-T8N` 설치위치 → `CP/EP/ED Inductrial Ethernet Switch` |
| 수정 | 712 | 전기 `15형 VIA-T150M-PT 산업용 터치모니터` 창고번호 → `72` |
| 수정 | 1126 | 외산(TAMS) `Power supply 24-28V AC/DC` 비고 → `모델명 : QT40` |

`apply_materials.py`로 반영했습니다(1회용, 반영 후 삭제. 필요하면 git 이력에서).

### 이어서 반영한 것 (같은 날)

| 작업 | 결과 |
|---|---|
| 미반영 구매 11건 | `purchase_history` id 816~826. 자재 연결 9건 82개는 재고 증가, CL60 50개(소모품)와 HMI 수리비 1건은 이력만 |
| 교체이력 신규 | 211건 삽입(자재 연결 187 / 이름만 24) + 기존 3건 정정 |
| 재고 동기화 | 58종을 엑셀 신본에 맞춤 (늘림 +127 / 줄임 −157) |

최종 확인(`compare_stock.py`): **DB = 신본 1,167종 + 이번구매 9종 + 합산자재 6종**,
설명 안 되는 잔여물 0. 남은 차이는 아래 보류 5종뿐입니다.

**DB 재고는 7/13 구본 상태에 멈춰 있었습니다.** 웹앱 출고 등록이 거의 안 쓰이고 있어서,
바뀐 자재만 뒤처져 있었습니다(안 바뀐 자재는 구본=신본이라 맞아 보였을 뿐입니다).

### 보류 — 실무자 확인 대기

**① 테일드럼 3종 (+27개) — 해결됨.** 2026-07-28에 20건 21개가 `보류`에서 `입고`로
바뀌면서 재고가 되돌아왔는데(인수자 장필교), **수리 보낸 부품이 돌아온 것**이라고
실무자가 확인해줬습니다.

```
6~7월  한진 자재로 테일드럼 교체        → 재고에서 빠짐 (보류)
       떼어낸 부품을 수리 보냄
7/28   수리 끝나고 돌아옴 (인수 장필교)  → '입고'로 바꾸고 재고 복구
```

비고의 "하자보수용 재고 소진 (9)(8)…(1)" 카운트다운은 그 수리품을 어디서 충당했는지를
적어둔 것입니다.

| 부품 | 조정 전 | 조정 후 | 비고 |
|---|---:|---:|---|
| Tail DRUM(Φ89 930W용 B/F 830) | 0 | 18 | 수리복귀 10 + 7월 구매 입고 8 |
| Tail DRUM(Φ89 1150W용 B/F 1050) | 7 | 15 | 수리복귀 |
| Tail Drum(Ø89 1150W용)_Shaft | 8 | 9 | 수리복귀 |

**우리 시스템의 "수리 관리 → 반납(정상복귀)"과 같은 흐름입니다.** 다만 그 20건은
2026-07에 엑셀에서 이관된 것이라 DB에 수리 건이 안 만들어져 있어서, 반납 처리로는
표현할 수 없습니다. 재고만 맞췄습니다.

**② ⚠️ `Belt 45 x ...` 33종이 벨트류와 외산(TAMS)에 이중으로 들어가 있음 — 미해결**

처음엔 `Belt 45 x 3265` 한 건인 줄 알았는데 **33종입니다.** 2026-08-18에 실무자가
**"벨트류와 외산(TAMS)에 있는 같은 것은 다 합쳐져 있다"**고 확인해줬습니다.

같은 BEUMER 벨트인데 자재목록에 두 줄씩 있습니다. 벨트류 쪽 `적용 C/V`에도
`BEUMER induction Belt ...`라고 적혀 있어 같은 물건이 맞습니다.

| | 외산(TAMS) | 벨트류 |
|---|---|---|
| 행 수 | 35 | 45 |
| 발주코드 | **있음** (`7232613` 등) | 없음 |
| 창고번호 | 있음 (137, 138 …) | 없음 |
| 현재재고 합계 | 58 | 71 |
| 사용열 합계 | 17 | 17 |

**재고가 최대 58개 부풀려져 있습니다.** 두 줄이 같은 실물을 각각 세고 있다면
자재목록 총합이 그만큼 많습니다.

**사용 기록도 같은 교체 건을 두 시트에 각각 적었습니다.** 양쪽 사용열 합계가 17로
똑같습니다. 그래서 `Belt 45 x` 8종의 사용량이 교체이력의 2배로 잡힙니다.
`Belt 45 x 1601`은 외산 쪽 재고가 **−4**인데, 사용 6을 양쪽에서 각각 뺀 결과로 보입니다.

**33종 중 28종은 양쪽 재고가 같은 값이고, 5종만 다릅니다.**

| 부품 | 벨트류 | 외산(TAMS) | 원본 행 (벨트류 / TAMS) |
|---|---:|---:|---|
| Belt 45 x 3265 | 6 | 1 | 104 / 156 |
| Belt 45 x 4161 | 5 | 3 | 114 / 166 |
| Belt 45 x 4587 | 3 | 1 | 116 / 168 |
| Belt 45 x 1601 | 2 | −4 | 90 / 141 |

> `Belt 45 x 4801`도 처음엔 다른 줄 알았는데(벨트류 2 / TAMS 4) **제가 잘못 센 것**입니다.
> TAMS에 행이 둘(169·170행)인데 **발주코드가 같고 창고만 165·166으로 다릅니다.**
> 행마다 2개씩이라 벨트류 2와 같습니다. 4절의 창고별로 나뉜 자재를 합친 사례와 같은 형태입니다.
> **한 시트에 같은 이름이 여러 행이면 무조건 더하지 말고 발주코드·창고번호를 먼저 볼 것.**

전체 33종 목록은 `docs/belt45_중복_33종.json`에 있습니다(부품별 양쪽 재고·사용열·원본 행번호).

### 다음에 할 일

1. **위 4종 실물을 셀 것.** 나머지 28종은 양쪽 값이 같아 그 값을 그대로 쓰면 됩니다.
2. **33종을 한 줄로 합칠 것.** 남길 쪽은 **외산(TAMS)** — 발주코드와 창고번호를 갖고
   있습니다. 벨트류를 남기면 발주할 때 코드를 다시 찾아야 합니다.
3. ⚠️ **현재재고를 합산하지 말 것.** 8-2의 합치기 규칙은 "현재재고 합산"인데,
   이번엔 같은 실물을 두 번 적은 것이라 더하면 두 배가 됩니다.
4. 교체이력 1건(2026-07-27 IU224 BLT8)이 이 이유로 아직 DB에 안 들어가 있습니다.

**되돌리기 어려운 작업이라 실물 확인 전에는 손대지 마세요.**

### ⚠️ 새로 발견 — history의 자재 연결이 거의 전부 끊겨 있음 (미해결)

**DB 출고 이력 4,246건 중 `material_id`가 채워진 것은 2건뿐입니다.**
그 2건은 웹앱에서 등록한 것이고, 7월에 엑셀에서 이관한 4,244건이 전부 비어 있습니다.
부품명이 안 맞아(`Ø` vs `Φ` 같은 글자 차이로 추정) 연결이 안 된 것으로 보입니다.

**증상:** 앱 사용이력 화면의 `부품명(규격)` 열이 거의 전부 비어 있습니다
(`db.js`가 `materials(part_name)` 조인으로 가져오기 때문). 자재별로 묶어 보거나
거르는 것도 안 됩니다. `part_memo`에 이름은 남아 있어 이력 자체가 사라진 건 아닙니다.

**주의:** 이것 때문에 중복 검사를 `material_id`로 하면 **하나도 안 걸립니다.**
2026-08-18에 이력 240건을 넣으려다 중복 2건만 걸러져서 발견했습니다.
`일자 + 부품명(part_memo) + 수량 + 설비ID`로 대조해야 합니다.

**할 일:** `part_memo`를 자재 이름과 대조해 4,244건의 `material_id`를 채우는 작업.
오늘 넣은 211건은 연결을 제대로 했으므로, 지금은 **새 것만 연결된 상태**입니다.

> 메모리에 "사용이력 4,245건 원본 대조 완료(2026-07-23)"로 남아 있지만
> **건수와 수량만 봤고 자재 연결은 안 봤습니다.** 검증 범위를 과신하지 말 것.

### 실무자 확인이 필요한 것

- **8월 구매분 11건이 자재현황 시트에 안 옮겨져 있음** (`CL60` 50, `LB-110"` 28,
  `LB-150"` 12, `B-222"` 10 등 26.08.07 우양물산 건). **약 133개**로, 이번 검산에서
  나온 가장 큰 실질 오차입니다.
- **체인 코수 표기 179개**(40종 179행). `#60-26코` 같은 기록이 자재목록의 체인
  (`HC60`, `CL60` 등)과 이름이 안 맞아 재고에서 아예 안 빠집니다. 차감 대상으로
  보느냐에 따라 현재재고가 **29개 과다** 또는 **116개 과소**로 갈립니다.
- **벨트류 8종 사용열이 이력의 2배** (8-8 참고).

### 재고를 이력으로 재계산해 보면 (2026-08-18)

`구본 현재재고 + 신규구매 − 신규소모`를 자재별로 구해 신본 현재재고와 대조했습니다.
**재고가 움직인 78종 중 56종이 정확히 일치**하고, 어긋나는 22종은 전부 아래 둘 중
하나입니다. 새로운 문제는 없습니다.

1. **위의 8월 구매 11건이 안 더해진 것** — 소모는 반영됐는데 구매만 빠져서 재고가
   실제보다 적게 나옵니다. 현재재고가 실제로 바뀐 자재 중 안 맞는 것은
   `LB-110"` `LB-150"` `S8M025-824` `S8M040-896` `B-166"` `Belt 45 x 3265`
   **6종뿐이고, 전부 이 11건에 걸린 품목입니다.** 11건만 들어가면 6종도 맞아떨어집니다.
2. **체인 코수 9종 약 18개** — 소모 기록은 있는데 대응 자재가 없어 어디서도 안 빠짐.

`CL60` `HC60`은 자재현황에 **현재재고 칸 자체가 비어 있어** 재고를 관리하지 않는
품목입니다. 구매 기록만 있고 재고가 안 움직이는 게 정상입니다.
